#By using recursive method
def search(arr, curr_index, key):
    if curr_index == -1:
        return -1
    if arr[curr_index] == key:
        return curr_index
    return search(arr, curr_index+1, key)
arr=[1,2,3,4,5]
result=search(arr,0,5)
if result!=-1:
    print(result)
else:
    print(result)
#without using recursion Method
def search(arr, x):
 
    for i in range(len(arr)):
 
        if arr[i] == x:
            return i
 
    return -1
arr=[1,2,3,4,5]
result=search(arr,5)
if result!=-1:
    print(result)
else:
    print(result)
#The time complexity of the above algorithm is O(n). 
#Auxiliary Space: O(1) for iterative and O(n) for recursive
